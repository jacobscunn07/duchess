---
phase: 08-footer-migration
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: []
autonomous: false
requirements: [LAYOUT-06, LAYOUT-07, LAYOUT-08]

must_haves:
  truths:
    - "The footer band at the bottom of the terminal shows the IAM ARN (or loading... / error) on the left and HH:MM:SS clock on the right"
    - "The footer does not show version, profile, region, refresh interval, or a service indicator ([S3] / [ECS])"
    - "The header still shows logo, profile, region, refresh, and version — Phase 7 regressions are absent"
    - "The layout is stable at 80x24 and at a wider terminal — no content overflow, no blank rows"
  artifacts: []
  key_links: []
---

<objective>
Visual verification that the refactored footer renders correctly at runtime. Automated tests confirm the contract at the string level; this checkpoint confirms it visually — correct layout, correct background, no regressions in the header.

Purpose: The footer background (Surface color), ARN truncation, and clock alignment are only fully verifiable in a real terminal.
Output: User approval or a list of issues to fix.
</objective>

<execution_context>
@/Users/jacobcunningham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobcunningham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-footer-migration/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of footer layout</name>
  <files></files>
  <action>No automated work — visual inspection required.</action>
  <verify>Human confirms footer shows ARN on left and clock on right, no service indicator, and header remains correct.</verify>
  <done>User approves the visual result or files issues for gap closure.</done>
  <what-built>Refactored status bar into a simplified footer: IAM principal ARN (or loading.../error) on the left, live HH:MM:SS clock on the right, all inline colors from DarkTheme tokens, service indicator removed.</what-built>
  <how-to-verify>
    1. Run: `cd /Users/jacobcunningham/_CODE/duchess && go run .`
    2. Observe the BOTTOM bar (footer):
       - Left side: Should show your IAM ARN (e.g., `123456789012 arn:aws:iam::...`) or "loading..." while credentials load
       - Right side: Should show the live clock in HH:MM:SS format
       - Absent: version string, profile name, region name, refresh interval, [S3] / [ECS] indicator
    3. Observe the TOP bar (header) — must be UNCHANGED from Phase 7:
       - Left: ASCII duchess logo
       - Right: profile, region, refresh, version
    4. Press Tab to switch between S3 and ECS panels — confirm the footer does NOT change to show [S3] or [ECS]
    5. Press `r` to open region switcher — confirm footer still renders correctly behind the overlay
    6. Resize the terminal — confirm no layout overflow or blank rows appear
  </how-to-verify>
  <resume-signal>Type "approved" if the footer looks correct, or describe any visual issues for gap closure.</resume-signal>
</task>

</tasks>

<verification>
- Visual: footer shows ARN on left and clock on right only
- Visual: header unchanged from Phase 7 (logo + metadata)
- Visual: Tab switching does NOT change footer content
- Visual: no layout overflow at any reasonable terminal size
</verification>

<success_criteria>
User confirms the footer renders correctly at runtime with IAM ARN on left, clock on right, and no service indicator. No Phase 7 header regressions visible.
</success_criteria>

<output>
After completion, create `.planning/phases/08-footer-migration/08-02-SUMMARY.md`
</output>
