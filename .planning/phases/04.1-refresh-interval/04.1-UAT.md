---
status: diagnosed
phase: 04.1-refresh-interval
source: 04.1-01-SUMMARY.md
started: 2026-03-05T22:50:00Z
updated: 2026-03-05T22:52:00Z
---

## Current Test

[testing complete]

## Tests

### 1. --refresh-interval flag changes panel refresh rate
expected: Run the app with a short interval, e.g. `duchess --refresh-interval 5`. The S3 and ECS panels should visibly poll/refresh every 5 seconds instead of the default 30 seconds.
result: issue
reported: "I can't tell if it's working or not"
severity: major

### 2. refresh_interval config key is respected
expected: Set `refresh_interval: 5` in the duchess config file and launch without the CLI flag. Panels should refresh on the configured cadence, identical behavior to using the flag.
result: issue
reported: "I can't tell if this is working"
severity: major

## Summary

total: 2
passed: 0
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "Running with --refresh-interval 5 causes panels to visibly refresh every 5 seconds"
  status: failed
  reason: "User reported: I can't tell if it's working or not"
  severity: major
  test: 1
  root_cause: "No UI element surfaces the refresh interval or last-refreshed time. The interval is wired correctly but completely invisible: no last-refreshed timestamp in breadcrumb, no interval shown in status bar. Transient spinner disappears too fast to observe."
  artifacts:
    - path: "internal/ui/s3/model.go"
      issue: "Model struct has no lastRefreshed time.Time field; renderBreadcrumb only shows spinner, nothing after refresh completes"
    - path: "internal/ui/ecs/model.go"
      issue: "Same — no lastRefreshed field, breadcrumb shows nothing between refreshes"
    - path: "internal/ui/status.go"
      issue: "renderStatusBar does not include refresh interval in left group"
  missing:
    - "Add lastRefreshed time.Time field to s3.Model and ecs.Model, set on data-loaded messages"
    - "Show last-refreshed timestamp (HH:MM:SS) in renderBreadcrumb after spinner disappears"
    - "Show refresh interval (e.g. '30s') in status bar left group"
  debug_session: ""

- truth: "Setting refresh_interval in config file causes panels to refresh at the configured cadence"
  status: failed
  reason: "User reported: I can't tell if this is working"
  severity: major
  test: 2
  root_cause: "Same root cause as test 1 — no observable UI feedback. The config value is read correctly but there is no status bar indicator showing the active interval, so the user cannot confirm the config key is being honored."
  artifacts:
    - path: "internal/ui/status.go"
      issue: "renderStatusBar does not display m.cfg.RefreshInterval anywhere"
    - path: "internal/ui/s3/model.go"
      issue: "No lastRefreshed timestamp in breadcrumb to help user correlate refresh cadence"
    - path: "internal/ui/ecs/model.go"
      issue: "Same"
  missing:
    - "Refresh interval shown in status bar (e.g. '| 30s') derived from m.cfg.RefreshInterval"
    - "lastRefreshed timestamp in panel breadcrumbs"
  debug_session: ""
