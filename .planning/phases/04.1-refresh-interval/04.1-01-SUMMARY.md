---
phase: 04.1-refresh-interval
plan: 01
subsystem: ui
tags: [bubbletea, tui, s3, ecs, refresh, configuration]

# Dependency graph
requires:
  - phase: 04-ecs-browsing
    provides: ECS panel four-state model and S3 panel with hardcoded tick durations
provides:
  - Parameterized S3RefreshTickCmd(d time.Duration) accepting user-configured duration
  - Parameterized ECSClusterRefreshTickCmd/ECSServiceRefreshTickCmd/ECSTaskRefreshTickCmd accepting duration
  - s3 and ECS panel Models with refreshInterval field threaded from cfg.RefreshInterval
  - Single int->Duration conversion in identityLoadedMsg handler wiring cfg.RefreshInterval end-to-end
affects: [phase-05-overlays, any future panel that uses refresh ticks]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Parameterized tick cmds: accept time.Duration instead of embedding literals"
    - "Single conversion point: int->time.Duration done once in root model handler, not at each call site"
    - "NewModel convention: refreshInterval is 5th parameter in both s3 and ecs panel constructors"

key-files:
  created: []
  modified:
    - internal/ui/s3/client.go
    - internal/ui/s3/model.go
    - internal/ui/ecs/client.go
    - internal/ui/ecs/model.go
    - internal/ui/model.go

key-decisions:
  - "refreshInterval stored on panel Model struct so all tick-rescheduling call sites use m.refreshInterval without threading extra arguments through message handlers"
  - "int->Duration conversion (time.Duration(m.cfg.RefreshInterval)*time.Second) done exactly once in identityLoadedMsg handler — not repeated at individual call sites"
  - "tickCmd() in model.go left unchanged — it drives the 1-second status bar clock and is unrelated to configurable refresh"

patterns-established:
  - "Tick cmd parameterization: func XxxRefreshTickCmd(d time.Duration) tea.Cmd pattern for all panel refresh ticks"
  - "Panel constructor refreshInterval threading: always pass as last constructor param after width/height"

requirements-completed: [CONF-01, CONF-02]

# Metrics
duration: 3min
completed: 2026-03-05
---

# Phase 4.1 Plan 01: Wire Configurable Refresh Interval Summary

**Replaced all five hardcoded S3/ECS tick durations (30s/10s/10s/5s) with parameterized time.Duration, threading cfg.RefreshInterval end-to-end from CLI flag through panel constructors to all 10 tick-rescheduling call sites.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-05T22:43:59Z
- **Completed:** 2026-03-05T22:46:23Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- S3RefreshTickCmd now accepts `d time.Duration` — hardcoded `30*time.Second` eliminated
- All three ECS tick cmds (Cluster/Service/Task) now accept `d time.Duration` — hardcoded 10s and 5s eliminated
- Both panel Models gain a `refreshInterval time.Duration` field; NewModel accepts it as 5th parameter
- `internal/ui/model.go` identityLoadedMsg handler converts `cfg.RefreshInterval` int to Duration once and passes to both panel constructors
- All 10 call sites (2 in s3/model.go, 8 in ecs/model.go) pass `m.refreshInterval`
- `go build ./...` and `go vet ./...` pass with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Parameterize S3 tick cmd and thread duration through s3 panel** - `da88088` (feat)
2. **Task 2: Parameterize ECS tick cmds, thread duration through ECS panel, wire root model** - `68df6db` (feat)

**Plan metadata:** *(docs commit follows)*

## Files Created/Modified
- `internal/ui/s3/client.go` - S3RefreshTickCmd signature changed to accept d time.Duration
- `internal/ui/s3/model.go` - Added refreshInterval field, updated NewModel, fixed 2 call sites
- `internal/ui/ecs/client.go` - All three ECS tick cmd signatures changed to accept d time.Duration
- `internal/ui/ecs/model.go` - Added refreshInterval field, added time import, updated NewModel, fixed 8 call sites
- `internal/ui/model.go` - identityLoadedMsg handler converts cfg.RefreshInterval int->Duration, passes to both NewModel calls

## Decisions Made
- `refreshInterval` stored on panel Model struct so all tick-rescheduling call sites use `m.refreshInterval` without threading extra arguments through message handlers
- `int->Duration` conversion done exactly once in `identityLoadedMsg` handler — not repeated at individual call sites
- `tickCmd()` in model.go left unchanged — it drives the 1-second status bar clock and is unrelated to configurable refresh

## Deviations from Plan

None - plan executed exactly as written. The only unplanned addition was importing `"time"` into `internal/ui/ecs/model.go` (it was missing; required by Rule 3 as a blocking issue for the new `time.Duration` field).

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing `time` import to internal/ui/ecs/model.go**
- **Found during:** Task 2 (adding refreshInterval time.Duration field to ecs Model struct)
- **Issue:** `time` package not imported in ecs/model.go; required for `time.Duration` type in struct field and NewModel parameter
- **Fix:** Added `"time"` to the import block
- **Files modified:** internal/ui/ecs/model.go
- **Verification:** go build ./... passes
- **Committed in:** 68df6db (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking — missing import)
**Impact on plan:** Necessary for compilation. No scope creep.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- CONF-01 and CONF-02 are complete: `--refresh-interval` CLI flag and `refresh_interval` config key now have full observable runtime effect on S3 and ECS panel refresh behavior
- The global 1-second status bar clock is unchanged
- Phase 5 (Overlays) can proceed; no refresh-interval concerns carry forward

---
*Phase: 04.1-refresh-interval*
*Completed: 2026-03-05*

## Self-Check: PASSED

- FOUND: internal/ui/s3/client.go
- FOUND: internal/ui/s3/model.go
- FOUND: internal/ui/ecs/client.go
- FOUND: internal/ui/ecs/model.go
- FOUND: internal/ui/model.go
- FOUND: .planning/phases/04.1-refresh-interval/04.1-01-SUMMARY.md
- FOUND commit: da88088 (Task 1)
- FOUND commit: 68df6db (Task 2)
