---
phase: 03-s3-browsing
plan: "03"
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - internal/ui/s3/model.go
autonomous: true
gap_closure: true
requirements: [S3-04]

must_haves:
  truths:
    - "Pressing / from the bucket list opens a filter bar with a 'Filter: ' prompt"
    - "Typing a substring and pressing Enter on the bucket list narrows the visible buckets to those whose names contain the substring"
    - "Pressing Esc with the filter bar open (on either bucket list or prefix list) dismisses the bar and restores the full previous list"
  artifacts:
    - path: "internal/ui/s3/model.go"
      provides: "S3 panel model with / filter enabled on both panelBucketList and panelPrefixList"
      contains: "m.state == panelPrefixList || m.state == panelBucketList"
  key_links:
    - from: "internal/ui/s3/model.go (case \"/\":)"
      to: "filterMode = true"
      via: "guard condition allows panelBucketList"
      pattern: "panelPrefixList \\|\\| m\\.state == panelBucketList"
    - from: "internal/ui/s3/model.go (case \"enter\": filterMode branch)"
      to: "list.SetItems(filtered savedItems)"
      via: "client-side substring filter when state == panelBucketList"
      pattern: "panelBucketList"
---

<objective>
Close UAT gap #8: filter mode must be reachable from the bucket list screen, not only from within a prefix list.

Purpose: The / key handler guards entry to filter mode with `m.state == panelPrefixList`, blocking activation on the bucket list entirely. Additionally, the enter-to-apply path unconditionally calls FetchPrefixCmd — which panics when bucketClient is nil (as it always is on the bucket list). Both defects must be fixed together for correct behavior.

Output: A single modified file (internal/ui/s3/model.go) where filter mode opens on both panelBucketList and panelPrefixList, and the Enter-to-apply path branches correctly: client-side substring filter for panelBucketList, FetchPrefixCmd for panelPrefixList.
</objective>

<execution_context>
@/Users/jacobcunningham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobcunningham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-s3-browsing/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen / guard and fix enter-to-apply branching in model.go</name>
  <files>internal/ui/s3/model.go, internal/ui/s3/model_test.go</files>
  <behavior>
    - Test: pressing / when m.state == panelBucketList sets m.filterMode = true and saves items to m.savedItems
    - Test: pressing / when m.state == panelPrefixList still sets m.filterMode = true (regression guard)
    - Test: pressing Enter with filterMode=true and state==panelBucketList applies a client-side substring filter — items whose name does not contain the filter string are excluded; no FetchPrefixCmd is issued (bucketClient remains nil)
    - Test: pressing Enter with filterMode=true and state==panelPrefixList still issues FetchPrefixCmd (regression guard)
    - Test: pressing Esc with filterMode=true restores m.savedItems and clears filterMode regardless of whether state is panelBucketList or panelPrefixList
  </behavior>
  <action>
Two precise edits to internal/ui/s3/model.go:

EDIT 1 — Widen the / activation guard (currently line 152):

  Before:
    case "/":
      if !m.filterMode && m.state == panelPrefixList {

  After:
    case "/":
      if !m.filterMode && (m.state == panelPrefixList || m.state == panelBucketList) {

No other changes to the / block.

EDIT 2 — Fix enter-to-apply to branch on state (currently lines 139-146):

  Before:
    case "enter":
      if m.filterMode {
        m.filterValue = m.filterInput.Value()
        m.filterMode = false
        m.loading = true
        filterPrefix := m.currentPrefix() + m.filterValue
        return m, FetchPrefixCmd(m.ctx, m.bucketClient, m.selectedBucket, filterPrefix)
      }

  After:
    case "enter":
      if m.filterMode {
        query := m.filterInput.Value()
        m.filterMode = false
        if m.state == panelBucketList {
          // Client-side substring filter — bucketClient is nil here; never call FetchPrefixCmd.
          filtered := make([]list.Item, 0, len(m.savedItems))
          for _, it := range m.savedItems {
            if s3it, ok := it.(s3Item); ok {
              if query == "" || strings.Contains(s3it.name, query) {
                filtered = append(filtered, it)
              }
            }
          }
          cmd := m.list.SetItems(filtered)
          m.savedItems = nil
          return m, cmd
        }
        // panelPrefixList: server-side prefix filter via S3 API.
        m.filterValue = query
        m.loading = true
        filterPrefix := m.currentPrefix() + m.filterValue
        return m, FetchPrefixCmd(m.ctx, m.bucketClient, m.selectedBucket, filterPrefix)
      }

The imports block already has "strings" — no import changes needed. The list import is already present.

After edits, run the existing test suite plus any new tests to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/jacobcunningham/_CODE/duchess && go test ./internal/ui/s3/... -v -run TestFilter 2>&1; go build ./... 2>&1</automated>
  </verify>
  <done>
    - `go build ./...` exits 0
    - `go test ./internal/ui/s3/...` passes all filter-related tests (including the new ones)
    - Filter mode activates from the bucket list when / is pressed
    - Enter applies a client-side substring filter on the bucket list without calling FetchPrefixCmd
    - PrefixList filter behavior is unchanged
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/jacobcunningham/_CODE/duchess && go build ./...` — exits 0, no compile errors
2. `go test ./internal/ui/s3/... -v` — all tests pass
3. `go vet ./...` — no issues
4. Manual smoke: launch duchess, press / from the bucket list screen — filter bar appears; type a substring; press Enter — list narrows to matching bucket names; press Esc — full list restores
</verification>

<success_criteria>
- go build succeeds
- All existing tests pass (no regressions)
- New filter tests for panelBucketList pass: / opens filter, Enter applies client-side substring match, Esc restores full list
- The single UAT gap (test #8, severity: major) is fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/03-s3-browsing/03-03-SUMMARY.md` following the standard summary template.
</output>
